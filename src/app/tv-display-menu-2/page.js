"use client";
import MakeItAMeal from "../../components/tv/proposal/MakeItAMeal";

/* Menu restructure proposal, screen 2. Nothing live depends on this — the
 * in-store boards remain /tv-display-1..4. */
export default function MenuProposal2Page() {
  return <MakeItAMeal />;
}
