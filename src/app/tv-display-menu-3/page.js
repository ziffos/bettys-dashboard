"use client";
import SharingBoxes from "../../components/tv/proposal/SharingBoxes";

/* Menu restructure proposal, screen 3. Nothing live depends on this — the
 * in-store boards remain /tv-display-1..4. */
export default function MenuProposal3Page() {
  return <SharingBoxes />;
}
