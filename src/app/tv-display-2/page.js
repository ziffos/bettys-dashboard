"use client";
import ShowcaseBoard from "../../components/tv/ShowcaseBoard";

/* In-store display 2. The spotlight is shared with the other showcase
 * screens — see components/tv/motionClock.js. */
export default function TvDisplay2Page() {
  return <ShowcaseBoard tvNumber={2} />;
}
