"use client";
import PickChicken from "../../components/tv/proposal/PickChicken";

/* Menu restructure proposal, screen 1. Nothing live depends on this — the
 * in-store boards remain /tv-display-1..4. */
export default function MenuProposal1Page() {
  return <PickChicken />;
}
