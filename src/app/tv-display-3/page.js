"use client";
import ShowcaseBoard from "../../components/tv/ShowcaseBoard";

/* In-store display 3. The spotlight is shared with the other showcase
 * screens — see components/tv/motionClock.js. */
export default function TvDisplay3Page() {
  return <ShowcaseBoard tvNumber={3} />;
}
