"use client";
import ShowcaseBoard from "../../components/tv/ShowcaseBoard";

/* In-store display 1. The spotlight is shared with the other showcase
 * screens — see components/tv/motionClock.js. */
export default function TvDisplay1Page() {
  return <ShowcaseBoard tvNumber={1} />;
}
