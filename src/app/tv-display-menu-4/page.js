"use client";
import BurgersExtras from "../../components/tv/proposal/BurgersExtras";

/* Menu restructure proposal, screen 4. Nothing live depends on this — the
 * in-store boards remain /tv-display-1..4. */
export default function MenuProposal4Page() {
  return <BurgersExtras />;
}
